This code takes in just 2 arguments by reading a standard.txt file and casting map of the characters in form of ascii art

## Usage 
To run the code type go run . "Hello" | cat -e

Note that Hello can be replaced any word, char or sign with the 95 characters in ascii